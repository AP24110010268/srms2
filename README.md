# srms2
A console-based **Student Report Management System (SRMS)** built in **C** to manage student records efficiently using **file handling**. Features include **login authentication**, **role-based access** (Admin, Staff, Guest), and operations like add, search, update, delete, and display. Ensures data persistence and security.
